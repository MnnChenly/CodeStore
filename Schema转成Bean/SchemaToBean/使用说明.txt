将xsd文件放在source目录下，然后打开terminal，将convertAll.sh拖入窗口运行即可。
所有生成的.m和.h文件会放在beans目录下。

使用生成的文件的时候还需要将Bean.h和Bean.m引入项目中。

存在一个问题，就是当两个xsd文件互相引用的时候会导致两个.h文件中互相@import，导致编译不通过，尽量避免两个xsd互相引用，或者生成后手动将两个@import中的一个移到.m文件中，再添加需要的@class

由于对schema的格式不是很了解，可能存在一些遗漏和不完善的地方，之后在使用中慢慢改进。